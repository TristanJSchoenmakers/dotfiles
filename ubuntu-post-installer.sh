#!/bin/bash
# A Simple Shell Script To setup your development environment
# Tristan J. Schoenmakers - 2022/06/26
# --------------------------------------------------------------------------------

# --------------------------------------------------------------------------------
# Apt packages
# --------------------------------------------------------------------------------

# General
sudo apt install curl

# Bluetooth & Audio
sudo apt install bsdutils
sudo apt install pulseaudio
sudo apt install pulseaudio-module-bluetooth

# Git
sudo apt install git
sudo apt install gh

# CLI
sudo apt install zsh
sudo apt install neofetch

# Docker
sudo apt install docker-ce-cli docker-ce docker-compose-plugin docker-desktop

# Dotnet
sudo apt install dotnet-sdk-6.0

# --------------------------------------------------------------------------------
# Snap packages
# --------------------------------------------------------------------------------

# Brave Browser
sudo snap install brave

# IDE: Visual Studio code
sudo snap install brave

# IDE: Rider 
sudo snap install discord

# Discord
sudo snap install discord

# Dotnet
sudo snap install dotnet-runtime-60
